Get a token for the client:
curl acme:acmesecret@localhost:8080/oauth/token -d grant_type=client_credentials

Get a token for the defaulecret@localhost:8080/oauth/token -d grant_type=password -d scope=read -d username=user -d password=<see log>t user
curl acme:acmes

curl acme:acmesecret@localhost:8080/oauth/check_token -d token=<>